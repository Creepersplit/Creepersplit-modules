const ver = require('../../config/htmlConfig.json')

var header = <h1>CreeperSplit</h1>

var version = ver

var logo = <h3>CreeperSplit Dashboard Now Loading...</h3>

module.exports = {
    header,
    version,
    logo
}